'use strict';

/**
 * @ngdoc function
 * @name corporateDashboardApp.controller:DataCtrl
 * @description
 * # DataCtrl
 * Controller of the corporateDashboardApp
 */
angular.module('corporateDashboardApp')
  .controller('DataCtrl', ['$state', function($state){
  }]);
